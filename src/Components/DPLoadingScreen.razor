@implements ITaggable

<DPOverlay IsVisible="@IsVisible">
    <DPLoadingIndicator Size="@LoadingIndicatorSize.Large" />
</DPOverlay>

@code {
    [Parameter]
    public bool IsVisible { get; set; }

    [Parameter]
    public object? Tag { get; set; }
}
